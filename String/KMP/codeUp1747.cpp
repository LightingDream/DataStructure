#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("in.txt", "r", stdin);
    char A[110], B[110];
    while(~scanf("%s%s", A, B)){
        printf("%s%s\n", A, B);
    }
    return 0;
}
